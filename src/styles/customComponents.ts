import { StyleSheet } from 'react-native';

export const customComponentStyles = StyleSheet.create({
  buttonDefault: {
    height: 50,
    justifyContent: 'center',
  },
});

export default customComponentStyles;
